// Copy to config.js and fill in. Both values are safe to publish: the anon
// key only grants what row-level security allows, and every policy is scoped
// to auth.uid(). The service-role key must NEVER appear here.
window.CONFIG = {
  supabaseUrl: 'https://xxxxxxxxxxxx.supabase.co',
  supabaseAnonKey: '',
};
